# macfanctl (WIP)
Macfanctl provides a library for interfacing with Apple SMC and a userspace daemon(`macfanctld`) that allows users to configure custom temperature curve response with interpolation.

Supports MacOS, Linux & Windows.